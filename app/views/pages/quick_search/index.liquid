{% liquid
  function current_profile = 'lib/current_profile', user_id: context.current_user.id
  assign limit = 3
  function records = 'lib/queries/quick_search/search', keyword: context.params.qkeyword, limit: limit, lang: context.language

  render 'theme/simple/quick_search/index', records: records, current_profile: current_profile, limit: limit, keyword: context.params.qkeyword
%}
