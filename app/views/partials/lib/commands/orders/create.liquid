{% liquid
  function object = 'lib/commands/orders/create/build', object: object, item: item, current_profile: current_profile
  function object = 'lib/commands/orders/create/check', object: object

  if object.valid
    function object = 'lib/commands/execute', object: object, mutation_name: 'orders/create'
  endif

  return object
%}
